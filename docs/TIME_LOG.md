# Time log

Budget: 8 focused hours. Clock started 2026-09-10 16:50 CEST.

**Total logged: 14.4 h — 4.5 h on the brief, 9.8 h on work beyond it** (the folder-tab interface ~5.0 h, the voices ~1.3 h, inferred answers ~1.2 h, Russian and Ukrainian with column-aware extraction ~1.5 h, quote sheets and the page viewer ~0.8 h). The core scenario was finished and evaluated inside the eight hours; the rest is depth I chose to add. See §15 of the delivery notes.

| # | Stage | Start | End | Notes |
|---|---|---|---|---|
| 1 | Scaffold, fixtures, expected.json | 16:50 | 16:58 | Fixtures generated from `fixtures/source` with pdf-lib |
| 2 | Loader, normalizer, indexer, core pipeline, unit tests | 16:58 | 17:04 | 20/20 unit tests; pdf.js 6 moved `destroy()` to the loading task |
| 3 | Eval harness | 17:04 | 17:06 | Written and type-checked; first run waits for the API key |
| 5 | UI + voice (started early while the key is pending) | 17:06 | 17:12 | Upload, limits and error states verified in the browser; answers need the key |
| 8a | README, pricing, delivery-notes draft | 17:12 | 17:16 | Prices checked on vendor pages |
| — | Discussion: API access | 17:16 | 17:35 | Anthropic key valid but balance is 0. Decision: develop on a free NVIDIA-hosted model now, final run on Haiku after top-up |
| 4b | Second LLM adapter (NVIDIA) | 17:35 | 19:20 | Wall-clock is inflated by waiting on the free endpoints during the model probe (two models hung until request timeouts). Chosen: Nemotron 3 Super with reasoning off — with reasoning on, its JSON degenerated into whitespace. First eval (S1): 2/2 pass |
| 3b | Full eval on NVIDIA | 19:22 | 19:27 | First attempt aborted on a 503 from the free endpoint after 6/6 passes; added adapter retries and unscored provider errors, restarted 19:23. Result: P0 100%, P1 75% (A6, A7 fail 3/3), holdout 100%, 0 critical |
| 6 | Fix failures, rerun | 19:27 | 19:31 | A7: spoken-letter normalization; A6: prompt. Rerun: P0 96.7%, P1 83.3%, 0 critical; A6 still fails; two mis-cited "12 units" caught by the validator. Next: retry hint with the lines that hold the number; rule for "is it allowed" questions |
| 6b | Retry hint, permission rule, final NVIDIA eval | 19:31 | 19:42 | Targeted rerun: R1b, A7 3/3. Final full run (e41c065): P0 100%, P1 87.5% (A6 0/3), holdout 100%, 0 critical |
| 8b | Worked AI-output check, delivery notes | 19:36 | 19:45 | `npm run verify-example` |
| 7 | Voice test with the user's own PDF; fixes | 19:45 | 19:55 | Overview question got not_found → prompt rule + test A9 (3/3); STT timing read 0 ms → measured from the last interim result |
| — | Discussion: multilingual TTS plan (Chatterbox) | 20:00 | 20:20 | Chosen: EN/RU/UA through the browser, TTS behind an interface; Chatterbox documented, not integrated |
| 9 | Languages (plan B) and redesign | 01:40 (Sep 11) | 02:30 | Core + tests L1–L6 (18/18); bento/folder redesign with impeccable, animate, emil-design-eng; H3 holdout regression found and partly fixed |
| 10 | Voice test RU/UA in Chrome (Oleh) | 02:19 | 02:23 | 8 voice questions; STT median 69 ms, speech end → first audio median 2.07 s |
| 11 | Answer history and grouped quotes as folder stacks | 02:35 | 02:50 | Shades of the answer lime by age, question on the tab, wheel only over the tab strip, view-only earlier answers; quotes one folder per page with Show all. Checked at 1280 and 375 px |
| — | Discussion: a "think better" mode | 02:50 | 03:05 | Inferred answers with a Why line, related lines for not-found answers, named slips, Think harder (Claude only); a wrong inference counts as critical |
| 12 | Think-better mode | 03:05 | 04:00 | Tests X1–X5 committed before their first run. Four full NVIDIA runs: my own prompt and validator changes caused two regressions (H4, R1); the eval caught both and they were fixed. One "critical" was an eval artifact (provider 429), and the harness now skips the rest of a broken session. Final `6f9f76b`: P0 96.7%, P1 88.9%, holdout 80%, RU/UA 100%, think-better 60%, 0 critical. Think harder is wired but not run (no Anthropic balance). UI checked in the browser through the DOM (the pane was hidden, so no screenshots) |
| 14 | Tile entrance animation (user request, several rounds) | 10:30 | 11:05 | Rise from below with stagger; distance, duration, curve and stagger tuned on request (64px/200px desktop, 600 ms, quadratic ease-in-out, 100 ms) |
| 15 | Fix after my CV test: wrong not-found answers, slow answers | 11:08 | 11:40 | Replayed my six Russian voice questions on my CV. The model chose the status before reading, and the old prompt failed the same way. The answer now starts with the restated question and a private analysis. The not-found check accepts any document again. Two-column pages are read column by column. CV replay: 6/6 answered. Full eval `627aaf7`: P0 96.7%, P1 92.6%, holdout 100%, RU/UA 94.4%, think-better 80%, 0 critical, retries 6%. An interleaved A/B shows the new format costs ~34 tokens and no measurable time |
| 16 | CV: the "Languages" and "Focus Areas" columns | 11:40 | 11:50 | Recursive column cuts: a section may hold columns of its own. Running text is required on both sides, so label–value tables stay whole, and a capitalised line never continues a wrapped one. Fixtures byte-identical, 44/44 unit tests |
| 17 | Quotes as paragraph sheets; the cited page with frames | 11:55 | 12:35 | Planned with a mockup; decisions by the user: group by paragraph, the real PDF page, no citation limit. Line positions are kept from the text layer. Each cited paragraph is a sheet, with the lines around it fading out. "Open in page" renders the PDF page with the paragraph lit and cited lines framed: a left panel on desktop, a bottom sheet on phones. 48/48 unit tests, fixtures byte-identical, checked in the browser |
| 18 | Zoom in the page viewer | 12:40 | 12:55 | 100–300% by −/+ (the level resets to fit), the keys + − 0, a pinch, or Ctrl/⌘ with scroll, around the point under the pointer. A gesture scales the image at once; the page is re-rendered sharp at rest into an offscreen canvas and swapped in together with its frames. The canvas stays under iOS Safari's size limit. Checked in the browser at every step |
| — | Discussion: OCR / document-AI models | 12:56 | 13:23 | Decision: no OCR in the pipeline. A parser seam with an OCR note, and a table fixture test, recorded for later in `docs/BACKLOG.md` |
| — | Discussion: folder tabs | 13:23 | 13:50 | Switching had no animation, colours followed positions, filed folders were as wide as the front one, and their tabs rose above it. Chosen: tabs keep their places (variant A) |
| 19 | Folder tabs keep their places | 13:50 | 16:08 | Worked 13:50–14:07 and 15:34–16:08 (51 min); the pause is not counted. Each tab has a fixed place in one row: bringing a folder forward changes only its depth, and its colour spreads from under its tab. Filed folders are 8px narrower a side and show 3px of edge each, and every tab starts on the front tab's line. Colours belong to folders. The wheel still scrolls the row without changing the open answer. Chrome's compositor misplaced a running clip-path circle, so the flood is a scaled disc. Checked in Playwright at 1280, 600 and 375 px, frame by frame at 1/20 speed |
| — | Discussion: a better voice | 16:26 | 16:35 | Compared hosted and open voices for English, Russian and Ukrainian, with the posts and repositories that drew the most response: the most-discussed open models do not cover Ukrainian. Found that the production voice priced in `pricing.md` (Deepgram Aura) has no Russian or Ukrainian |
| — | Discussion: ElevenLabs or Google; the API key | 19:04 | 19:41 | Worked 19:04–19:09 and 19:39–19:41 (7 min). ElevenLabs chosen over Gemini TTS, whose first audio took 10–20 s with personal keys in July–August 2026 (Google forum). Key: Text to Speech only, a credit limit, no IP restriction, because Vercel has no fixed address |
| 20 | Blind voice listening test | 20:56 | 21:02 | ElevenLabs Flash v2.5 and v3, two voices, three languages, against the macOS voices, on a page with shuffled letters. The free plan refuses library voices through the API. Chosen: Flash v2.5 |
| 21 | Neural voice in the app | 21:17 | 21:36 | `/api/tts` streams ElevenLabs PCM into Web Audio, and speaks only answer text that `/api/answer` signed. The browser's voice is the fallback; a forced failure was checked. Headless Chromium: first byte median 285 ms, answer → first sound 349 ms. Aura dropped from `pricing.md`. 52/52 unit tests |
| — | Discussion: free voices | 21:38 | 21:42 | Open models that speak Ukrainian: Fish Audio S2 Pro needs a 24 GB GPU and a commercial license; Piper, Silero and StyleTTS2 Ukrainian need a server or sound like the system voices; only Supertonic 3 runs in a browser. Chosen after listening to its samples: Supertonic 3 as a third, on-device voice |
| 22 | On-device voice (Supertonic 3) and the voice switch | 23:04 | 23:27 | A Web Worker runs the model with ONNX Runtime Web (WebGPU, WebAssembly as the last resort). The model comes from Hugging Face and stays in the browser's cache. The voice switch reads Built-in · ElevenLabs · On device. Benchmark on this Mac: WebGPU about 9× faster than WebAssembly; padding inputs to fixed lengths cut an answer from 2.4 s to 1.1 s. 55/55 unit tests |
| — | My Chrome test of the on-device voice on my CV | 23:35 | 23:37 | The answers were correct; the voice started 2.7–5.4 s after each one. Cause: long single sentences synthesized whole, and every new input length building WebGPU kernels |
| 23 | On-device voice: first sound in under a second | 23:37 | 23:55 | A short first piece and one fixed, pre-warmed shape for each kind of piece; a newer answer cancels between denoising steps. In the app it was still 1.5–2.1 s, because the tiles' drop-shadow filters were recomputed every frame while the answer animated. Tiles and tabs now cast box-shadows, and the first piece takes 0.74–0.79 s. 55/55 unit tests |
| 24 | The tab shadow bug; the voice kept fast | 23:57 | 01:55 (Sep 12) | Reported by the user: the fillets were out of step with the tabs, and a tab was clipped under the pointer. Cutting the tab's shadow at its foot had left a hard edge, and the fillets cast none. Putting the filter on a layer under the content was clean but slow again (1.5–2.2 s): a filter is recomputed while content moves over it too. The shadow is now painted in layers — the surface's shadow, the tab with its own, then the surface over the tab's foot. The fillets reach a pixel into the folder's edge, a hovered folder lifts and brightens whole, and the tab line has room for й, ї, у and д. First piece 0.75–0.87 s. 55/55 unit tests; checked in Playwright at three times the pixel density |
| 25 | The tab's S-curve at every depth | 01:58 | 02:30 | Reported by the user, zoomed in: a filed tab's convex corner and its concave fillet no longer fit in the height the tab had left, so the outline broke into a step. The tab kept a fixed 28px while its folder's edge rises 3px for every folder in front of it. The fixed height is gone: a tab reaches from the row's line down to its own edge, and its corner and fillet are each half of that. Depth and the hover lift are two registered lengths (`@property`), so height, corners and edge are computed from the same interpolated value every frame: the worst gap between a tab's foot and its edge during a reorder is 0.01px, against 1.3px before. Under the pointer a folder now lifts whole, tab included. The on-device voice is unaffected: median 866 ms against 917 ms measured on the old CSS with the same six questions. 55/55 unit tests |
| 26 | Shades of the answer lime | 02:30 | 02:50 | Reported by the user: the front card and the tab beside it read as one folder. A step of age was 8% towards the olive, 0.027 of oklab lightness; 13% (0.045) was still not enough to his eye, so a step is a fifth of the way, 0.069 — measured in the browser as 0.897 · 0.828 · 0.760 · 0.691 across the row. It stops after four steps, where ink on a tab still holds 4.9:1; deeper falls under the 4.5 floor, and the row carries four tabs at a time. The sage sheets keep their own step, which already reads apart at 0.036 |
| 27 | Colour belongs to the plane | 02:50 | 03:10 | Decided by the user, after two goes at a stronger ramp by age: a folder no longer carries a colour at all. The front plane carries the full tone and each plane behind it is a quarter of the way deeper (0.086 of oklab lightness for the answer lime), so bringing a folder forward brightens it and the one it replaces darkens. The flood now means that move: the card starts on the arriving folder's old plane and the front tone spreads from under its tab. Quote sheets and the dark folders of related lines follow the same rule. Row measured: 0.897 · 0.811 · 0.725 · 0.639, deepest ink 5.6:1. 55/55 unit tests |
| 28 | Deployment check, measurements on the deployed demo, документы | 03:10 | 04:05 | The deployed demo answered nothing: no model key on Vercel, so `/api/answer` returned 500 and `/api/tts` 404. After the keys were set: 15 typed questions on the deployed demo (M1, M4, M5 × 5), submit → first audio median 2.29 s, max 4.98 s; ElevenLabs first byte median 425 ms; ingestion 153 ms. Delivery notes corrected where they no longer matched the build: the summary claimed Claude Haiku answers, the runtime section led with it, the folder-colour rule was the old one, and the draft banner and the demo link were stale. Time split (4.5 h on the brief, 9.8 h beyond) written into §15 and this log's header |
| 29 | The answer's language, and the checks the notes claimed | 09:25 | 10:05 | The drift found in my own test is fixed: the answer is checked against the language asked for — letters and everyday words exclusive to one of the two Cyrillic languages, Cyrillic against Latin for English — and a slip costs one more call. A second slip keeps the answer and reports it, because a right answer in the wrong language beats no answer. Test L7 (Russian turn, then Ukrainian) committed before its first run: 3/3, and the other language sessions stayed 18/18. §12 claimed a check by eye that had not happened: the cited page was opened in the viewer, which renders the PDF with pdf.js, and the framed line compared with the quote word for word. §4 now names ChatGPT and Gemini, used for the first draft of my working spec. 62 unit tests |
| 30 | Three full evals, and what they cost me in certainty | 10:05 | 10:45 | Ran the whole suite three times inside half an hour on the same code. P0 came out 96.7%, 90.0% and 93.3%; the criterion of §7.8 is met once and missed twice, both times on R1b, with 0 critical failures every time. Nothing changed between the passes but the model's sampling. The first pass also caught a defect in my own language guard — a validated answer thrown away because the retry went on the language — and that is fixed. Two prompt attempts at R1b were reverted: the first made the clarifying question vaguer, the second traded R1b for R1. The notes now report all three passes, not the best one |
